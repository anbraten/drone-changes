{
  "name": "drone-changes",
  "version": "1.0.0",
  "description": "Check git diff for changes",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest tests/*.test.js",
    "build": "yarn ncc build src/index.js -o dist"
  },
  "dependencies": {
    "minimatch": "^3.0.4"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.22.3",
    "jest": "^26.2.2",
    "nodemon": "^2.0.4"
  }
}
